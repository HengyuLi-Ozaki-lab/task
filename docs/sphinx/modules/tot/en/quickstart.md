# Quickstart notebook

```{note}
An executable quickstart notebook for the `tot` module has not yet been
prepared. Once it is, it will live at
`docs/sphinx/shared/notebooks/tot-quickstart.ipynb` and be linked from
this page.

Until then, please refer to the minimal example in {doc}`hello-world`
and the registered-parameter reference in {doc}`parameters`.
```
