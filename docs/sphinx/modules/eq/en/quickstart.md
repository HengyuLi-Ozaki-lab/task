# Quickstart notebook

The notebook below walks through the end-to-end flow of the `eq`
module (`init` → `set_params` → `validate` → `run(mode=1)` →
`get_state` → ψ-surface plot).

```{note}
At build time `nb_execution_mode = "off"`, so the notebook is rendered
from pre-executed cell outputs. To re-run it manually, use
`jupyter nbconvert --to notebook --execute --inplace
docs/sphinx/shared/notebooks/eq-quickstart.ipynb`.
```

```{toctree}
:maxdepth: 1

eq-quickstart
```
