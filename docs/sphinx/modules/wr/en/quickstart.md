# Quickstart notebook

```{note}
An executable quickstart notebook for the `wr` module is not yet
available. Once it is, it will live at
`docs/sphinx/shared/notebooks/wr-quickstart.ipynb` and be linked from
this page.

Until then, see the minimal example in {doc}`hello-world` and the
parameter reference in {doc}`parameters`.
```
