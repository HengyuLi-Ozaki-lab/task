# クイックスタート notebook

```{note}
`ti` モジュール用の実行可能 quickstart notebook はまだ整備されていません.
作成された段階で `docs/sphinx/shared/notebooks/ti-quickstart.ipynb` に
配置され, ここからリンクされる予定です.

それまでは {doc}`hello-world` の最短例 + {doc}`parameters` の登録パラ
メータリファレンスを参照してください.
```

`tr` 側の notebook は実行可能形式で公開されています — ti も同じ流れで
書ける範囲なら参考になります.
