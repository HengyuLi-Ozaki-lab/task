# Quickstart notebook

```{note}
An executable quickstart notebook for the `ti` module is not yet
available. Once it is created it will live at
`docs/sphinx/shared/notebooks/ti-quickstart.ipynb` and be linked from
this page.

Until then, please refer to the minimal example in {doc}`hello-world`
and the registered-parameter reference in {doc}`parameters`.
```

The `tr` notebook is published in executable form — `ti` follows the
same flow within the scope of what each module supports, so it can
serve as a reference.
