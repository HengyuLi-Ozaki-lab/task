# Quickstart notebook

```{note}
A runnable quickstart notebook for the `fp` module is not yet
available. Once it is, it will live at
`docs/sphinx/shared/notebooks/fp-quickstart.ipynb` and be linked from
this page.

Until then, please refer to the minimal example in {doc}`hello-world`
and the registered-parameter reference in {doc}`parameters`.
```
