# クイックスタート notebook

```{note}
`fp` モジュール用の実行可能 quickstart notebook はまだ整備されていません.
作成された段階で `docs/sphinx/shared/notebooks/fp-quickstart.ipynb` に
配置され, ここからリンクされる予定です.

それまでは {doc}`hello-world` の最短例 + {doc}`parameters` の登録
パラメータリファレンスを参照してください.
```
