# Quickstart notebook

```{note}
An executable quickstart notebook for the `wrx` module is not yet ready.
Once it is, it will be placed at
`docs/sphinx/shared/notebooks/wrx-quickstart.ipynb` and linked from this
page.

In the meantime, see the minimal example in {doc}`hello-world` and the
parameter reference in {doc}`parameters`.
```
