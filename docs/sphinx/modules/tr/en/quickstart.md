# Quickstart notebook

The notebook below shows the end-to-end flow of the `tr` module
(`init` → `set_params` → `validate` → `run` → `get_state` → plot).

```{note}
At build time `nb_execution_mode = "off"`, so the notebook uses the
pre-executed cell outputs. To re-execute manually, run
`jupyter nbconvert --to notebook --execute --inplace
docs/sphinx/shared/notebooks/tr-quickstart.ipynb`.
```

```{toctree}
:maxdepth: 1

tr-quickstart
```
