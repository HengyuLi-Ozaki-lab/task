"""wrx_mcp: FastMCP server exposing TASK/WRX ray-tracing code as MCP tools.

Thin wrapper over :mod:`wrxlib` (ctypes binding to ``libwrxapi.so``).
Sister server to :mod:`tr_mcp` — same shape, different backing library.

See :mod:`wrx_mcp.server` for the tool implementations.
"""
